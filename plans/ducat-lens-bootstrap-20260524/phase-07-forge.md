# Phase 07 — Scaffold frontend + backend + ducat data bundle

## Owner

Forge 🔨 (Implementation Agent)

## Pre

- Phase 03 complete: `_architecture.md` locked (vision=RapidOCR, ducat=WFCD/warframe-items build-time, layout, deps)
- Pre-Forge sync gate passed: `git rev-list HEAD..origin/main --count` returned 0
- Working tree clean of unrelated changes
- New feature branch created by Herald 📯 (Release Manager) before Forge starts
- Internet available (script needs to fetch WFCD JSON from GitHub raw URLs)

## Reads

- `plans/ducat-lens-bootstrap-20260524/_architecture.md` — full lock (treat as immutable contract)
- `plans/ducat-lens-bootstrap-20260524/_research-ducat-api.md` — WFCD JSON URL pattern + sample code (build script reference)
- `plans/ducat-lens-bootstrap-20260524/_research-vision.md` — RapidOCR sample stub (not invoked in this phase, but informs backend module layout)
- `CLAUDE.md` Workspace map + Coding discipline section

## Writes

### Frontend scaffold (`frontend/`) — Astro + React islands (SEO HIGH)
- `frontend/package.json` — deps: astro ^5, @astrojs/react latest, @astrojs/sitemap latest, react 18, react-dom 18, react-dropzone latest; dev: @types/react, @types/react-dom, typescript 5.x
- `frontend/astro.config.mjs` — `defineConfig({ integrations: [react(), sitemap()], site: 'https://ducat-lens.example', output: 'static', server: { port: 4321, proxy: { '/analyze': 'http://localhost:8000' } } })` — NOTE: Astro dev server doesn't natively proxy. If proxy not supported in dev config, document workaround in README (env var `PUBLIC_API_BASE` for backend URL, fetch directly with CORS allowed from :4321 in backend main.py)
- `frontend/tsconfig.json` — extends `astro/tsconfigs/strict`
- `frontend/public/robots.txt` — `User-agent: *` allow all, `Sitemap: https://ducat-lens.example/sitemap-index.xml`
- `frontend/src/env.d.ts` — `/// <reference types="astro/client" />`
- `frontend/src/layouts/BaseLayout.astro` — shell: `<html lang="en">`, `<head>` includes `<SEO />`, `<body><slot /></body>`. Accepts props for title/description/ogImage
- `frontend/src/components/SEO.astro` — reusable head meta: title, description, canonical link, Open Graph (og:title/description/image/url/type), Twitter cards (twitter:card/title/description/image), and an optional JSON-LD `WebApplication` schema script block
- `frontend/src/pages/index.astro` — landing page using BaseLayout, contains crawlable substance: `<h1>ducat-lens</h1>`, one-paragraph description ("Upload a Warframe Ducat Kiosk screenshot and instantly see ducat values + sell recommendations for every Prime part you own."), bullet list of features, embedded image preview (alt-texted) of test fixture, and the `<DucatAnalyzer client:load />` React island. JSON-LD `WebApplication` schema in head via SEO component props.
- `frontend/src/components/DucatAnalyzer.tsx` — React island stub: empty functional component returning a placeholder `<div>Phase 09 will implement upload + results table here</div>`. Phase 09 fills in react-dropzone upload + fetch to `/analyze` + results table.
- Astro CORS note: backend `main.py` CORS middleware allows `http://localhost:4321` (Astro default dev port) in addition to or replacing `http://localhost:5173`. Cipher decision: replace 5173 with 4321 in this scaffold revision.

### Backend scaffold (`backend/`)
- `backend/pyproject.toml` — project metadata + `[project.dependencies]` block listing: fastapi, `uvicorn[standard]`, python-multipart, pillow, rapidocr-onnxruntime, httpx, pydantic; `[project.optional-dependencies.dev]`: pytest, pytest-asyncio, ruff, mypy
- `backend/main.py` — FastAPI app with single placeholder route `GET /` returning `{"status": "ok"}`; CORS middleware allowing `http://localhost:4321` (Astro dev port) for dev
- `backend/analyze.py` — empty module stub with docstring "OCR + ducat lookup + recommendation pipeline — implemented in phase 08"
- `backend/ducats.py` — `load_ducats()` function that reads `data/ducats.json` and returns `dict[str, int]`; raises FileNotFoundError with helpful message if file missing (points to `python -m backend.scripts.fetch_ducats`)
- `backend/__init__.py` — empty (package marker)
- `backend/scripts/__init__.py` — empty (package marker)
- `backend/scripts/fetch_ducats.py` — CLI script that downloads 5 WFCD JSON files (Warframes, Primary, Secondary, Melee, Companions) via httpx (sync mode), filters Prime-only items, walks `components` arrays, extracts `(component_name_lowercase → ducats)` pairs, writes merged dict to `data/ducats.json`. Print summary line `wrote N items to data/ducats.json`. Exit non-zero on any HTTP failure.

### Data bundle (`data/`)
- `data/ducats.json` — generated by running `python -m backend.scripts.fetch_ducats` once during phase 07 execution. Must NOT be hand-edited. Forge runs the script and verifies output exists + is valid JSON + has ≥ 100 entries (sanity check — WFCD has hundreds of Prime parts across the 5 categories).
- `data/.gitkeep` — remove if exists (data dir no longer empty)

### README update
- Append a new section `## Setup` to `README.md` documenting:
  - `cd frontend && pnpm install && pnpm dev` (boots Astro dev server on :4321)
  - `cd backend && python -m venv .venv && source .venv/bin/activate && pip install -e .` (installs backend)
  - `python -m backend.scripts.fetch_ducats` (one-time ducat data fetch — re-run on Warframe updates)
  - `uvicorn backend.main:app --reload` (boots FastAPI on :8000)
  - `cd frontend && pnpm build` (Astro static build for SEO crawlable HTML output)

## Steps

1. **Confirm pre-state**: read `_architecture.md` fully. If any section ambiguous, halt and ask Cipher 🔓 (Dev-Team Orchestrator) before writing any file.

2. **Scaffold backend first** (lighter dep tree, faster sanity check):
   - Write all `backend/*.py` + `backend/scripts/*.py` + `backend/pyproject.toml`
   - `cd backend && python -m venv .venv` (do NOT activate or pip install — that is dev-environment setup, document in README only)
   - Document install command in README; do not execute pip install yourself (the file outputs are the artifact, not an installed env)

3. **Run the build-time fetch script**:
   - Use Bash to run `python backend/scripts/fetch_ducats.py` (requires httpx in current Python env — if not present, install via `pip install httpx` in a one-off scope OR document that the user must `cd backend && pip install -e .` first then re-run; choose simplest path that gives a valid `data/ducats.json` artifact for this PR)
   - Verify `data/ducats.json` exists and contains ≥ 100 entries (load + `len(...)` check)
   - Verify a known sample: `"wisp prime blueprint"` should map to `100` (per `_research-ducat-api.md` confirmed sample table)

4. **Scaffold frontend (Astro + React islands)**:
   - Write `frontend/package.json`, `astro.config.mjs`, `tsconfig.json`, `public/robots.txt`, `src/env.d.ts`, `src/layouts/BaseLayout.astro`, `src/components/SEO.astro`, `src/components/DucatAnalyzer.tsx`, `src/pages/index.astro`
   - SEO must be wired (per `_architecture.md` SEO requirements section): `<SEO />` rendered in `<head>`, JSON-LD `WebApplication` schema in landing page, canonical link, og + twitter meta
   - Do NOT run `pnpm install` — Atrium 🏛️ (Frontend Architect) audit owns install for dep verification

5. **Update README**: append `## Setup` section per Writes spec.

6. **Self-verify**: read every file just written, confirm imports resolve mentally (no typos in module paths), confirm React component renders the heading.

7. **Report back to Cipher 🔓 (Dev-Team Orchestrator)** with file count + paths written, `data/ducats.json` entry count, and any deviations from `_architecture.md`.

## Output

- All scaffold files listed in Writes section, syntactically valid
- `data/ducats.json` exists, contains ≥ 100 entries, sample `wisp prime blueprint` → `100` verified
- `README.md` has `## Setup` section
- Zero AI attribution in any written file (per CLAUDE.md commit/PR hygiene HARD RULE)

## Gate

- Bastion 🧱 (Backend Architect) audits `backend/**/*.py` — returns `[PASS]`
- Atrium 🏛️ (Frontend Architect) audits `frontend/**/*.{ts,tsx}` + `package.json` — returns `[PASS]`
- Crucible 🔥 (Test Architect) — N/A for phase 07 (no tests yet; will gate phase 08)
- Inquisitor 🔎 (PR Reviewer) cross-file pre-PR review — returns `[PASS]` or `[ADVISORY]`
- `data/ducats.json` schema sanity: keys are lowercased strings, values are positive integers, dict has ≥ 100 entries

## Abort conditions

- WFCD GitHub raw URLs return non-200 — halt, report URL + status, do NOT commit a partial `data/ducats.json`
- Any dep listed in `_architecture.md` is not pip-installable in current Python version — halt, report version + error, route back to Cipher for dep pin update
- Sample value mismatch (`wisp prime blueprint` ≠ `100`) — halt, dump raw WFCD response, report to Cipher (signals upstream schema change)
- Frontend `package.json` would require Node version > what user has — halt, report required version

## MCP whitelist/blacklist

- Allowed: `context7` for FastAPI / Pydantic / React / Vite latest-API spot-checks
- Forbidden: `chrome-devtools` (no live UI to inspect yet; phase 09)
