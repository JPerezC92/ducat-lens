# Phase 03: Install deps + add shadcn components

## Owner

Forge 🔨 (Implementation Agent)

## Pre

- Phase 02 complete: Warden 🔒 (Dependency Warden) returned PASS or ADVISORY (user accepted)
- Working directory: `D:\projects\ducat-lens\frontend\`

## Reads

- Plan §Resolved decisions for `@tanstack/react-table` exact pin
- `frontend/components.json`: shadcn config; confirms `radix-nova` style + paths

## Writes

- `frontend/package.json`: add `@tanstack/react-table@<pin>`
- `frontend/pnpm-lock.yaml`: regenerated by pnpm
- `frontend/src/components/ui/table.tsx`: NEW via shadcn CLI
- `frontend/src/components/ui/badge.tsx`: NEW via shadcn CLI
- `frontend/src/components/ui/skeleton.tsx`: NEW via shadcn CLI
- `frontend/src/components/ui/card.tsx`: NEW via shadcn CLI

## Steps

1. `cd D:\projects\ducat-lens\frontend`
2. `pnpm add @tanstack/react-table@<exact-pin-from-phase-01>`; exact pin only, NO range operators
3. `pnpm dlx shadcn@4.8.0 add table`; accept default; verify file lands at `src/components/ui/table.tsx`
4. `pnpm dlx shadcn@4.8.0 add badge`
5. `pnpm dlx shadcn@4.8.0 add skeleton`
6. `pnpm dlx shadcn@4.8.0 add card`
7. Verify `frontend/package.json` after each install; all deps must be exact pins; manually strip any `^`/`~` if pnpm wrote ranges
8. Confirm each `ui/<name>.tsx` imports `cn` from `@/lib/utils` (proves path alias still works)
9. Confirm no `.dark` block reintroduced to `global.css`; shadcn add may try to extend `:root` / `.dark`; if it does, strip the `.dark` block again per CSS conventions locked phase Forge cleanup (2026-05-25)

## Output

- Updated `frontend/package.json` with tanstack pin
- 4 new shadcn component files
- Updated `frontend/pnpm-lock.yaml`

## Gate

- All 5 new deps/components in place
- `package.json` shows tanstack pin exact (no `^`/`~`/`>=`)
- `pnpm build` exits 0 (smoke test before next phase)
- `global.css` still has no `.dark` block

## Abort conditions

- `pnpm add` fails (peer dep error, network) → halt, report to Cipher 🔓 (Dev-Team Orchestrator)
- shadcn add fails (CLI error, missing config) → halt, inspect components.json
- `pnpm build` exits non-zero → halt, report full build log
- shadcn add reintroduces `.dark` block in `global.css` → strip it before phase complete; report restoration in step output

## MCP whitelist/blacklist

- Allowed: Bash for `pnpm add`, `pnpm dlx shadcn add`, `pnpm install`, `pnpm build`
- Forbidden: any git operation (Herald 📯 (Release Manager) owns commits)
